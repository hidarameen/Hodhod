# Telegram Auto-Forward Bot 🤖

نظام متكامل لتوجيه الرسائل تلقائياً من Telegram مع دعم الذكاء الصناعي ومعالجة الفيديو.

## المميزات الرئيسية ✨

### 🚀 التوجيه التلقائي
- دعم عدة مصادر وعدة أهداف في مهمة واحدة
- معالجة الرسالة مرة واحدة لتجنب التكرار
- التشغيل المتوازي لجميع المهام
- نظام Workers و Queues متقدم

### 🤖 الذكاء الصناعي
- دعم متعدد للمزودين:
  - OpenAI (GPT-4, GPT-3.5)
  - Groq (Mixtral, LLaMA 2)
  - Claude (Opus, Sonnet, Haiku)
  - HuggingFace (LLaMA 2, Mixtral)
- قواعد AI منفصلة لكل مهمة
- تلخيص النصوص تلقائياً
- تحويل الفيديو إلى نص + تلخيص

### 🎥 معالجة الفيديو
- تحويل الفيديو إلى صوت
- استخراج النص بواسطة Whisper
- تلخيص النص المستخرج بالذكاء الصناعي
- معالجة متوازية بدون تأخير

### 📊 الإحصائيات والسجلات
- إحصائيات مفصلة لكل مهمة
- سجل أحداث شامل
- تتبع الأخطاء المتقدم
- لوحة تحكم ويب حديثة

### 🔒 الأمان والإدارة
- نظام تسجيل دخول آمن
- إدارة متعددة للمشرفين
- يعمل فقط في المحادثات الخاصة
- التحقق من صلاحيات المشرفين

## البنية المعمارية 🏗️

```
├── telegram_bot/           # بوت Python مع Pyrogram
│   ├── handlers/          # معالجات الأوامر
│   ├── services/          # خدمات البوت
│   │   ├── ai_providers.py    # مزودي الذكاء الصناعي
│   │   ├── forwarding_engine.py # محرك التوجيه
│   │   ├── video_processor.py   # معالج الفيديو
│   │   └── queue_system.py     # نظام الطوابير
│   ├── utils/             # أدوات مساعدة
│   │   ├── database.py        # قاعدة البيانات
│   │   └── error_handler.py   # معالج الأخطاء
│   └── main.py           # الملف الرئيسي
│
├── server/                # API Backend بـ Node.js
│   ├── routes.ts         # API endpoints
│   └── storage.ts        # إدارة قاعدة البيانات
│
├── client/                # واجهة ويب React
│   └── src/
│       ├── pages/        # صفحات التطبيق
│       └── components/   # مكونات واجهة المستخدم
│
└── shared/               # أنواع مشتركة
    └── schema.ts         # Drizzle ORM schemas
```

## التثبيت والإعداد 🛠️

### الخيار 1: Docker (موصى به للإنتاج) 🐳

#### المتطلبات
- Docker 20+
- Docker Compose v2+

#### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd botnexus
```

2. **إعداد متغيرات البيئة**
```bash
cp .env.example .env
# قم بتعديل الملف .env وأضف المفاتيح الخاصة بك
```

3. **بناء وتشغيل التطبيق**
```bash
# بناء وتشغيل باستخدام Docker Compose
docker-compose up -d

# أو بناء الصورة يدوياً
docker build -t botnexus .
docker run -p 5000:5000 --env-file .env botnexus
```

4. **التحقق من التشغيل**
```bash
# عرض السجلات
docker-compose logs -f app

# التحقق من الصحة
curl http://localhost:5000/health
```

#### النشر على Northflank / Railway / Render

1. أنشئ مشروع جديد
2. اربط المستودع بالمشروع
3. أضف متغيرات البيئة:
   - `DATABASE_URL`
   - `BOT_TOKEN`
   - `API_ID`
   - `API_HASH`
   - `SESSION_SECRET`
   - مفاتيح API للذكاء الصناعي (حسب الحاجة)
4. النشر سيتم تلقائياً باستخدام Dockerfile

---

### الخيار 2: التثبيت المحلي

#### متطلبات النظام

- Node.js 20+ و npm
- Python 3.11+
- PostgreSQL
- ffmpeg (لمعالجة الفيديو)

#### تثبيت المكتبات

**Node.js**
```bash
npm install
```

**Python**
```bash
cd telegram_bot
pip install -r requirements.txt
```

### 3. إعداد البيئة

أنشئ ملف `.env` في المجلد الرئيسي:

```env
# Database
DATABASE_URL=postgresql://user:pass@localhost:5432/dbname

# Telegram Bot (في telegram_bot/.env)
BOT_TOKEN=your_bot_token
API_ID=your_api_id
API_HASH=your_api_hash
BOT_ADMIN_ID=your_telegram_id

# AI Providers
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk_...
ANTHROPIC_API_KEY=sk-ant-...
HUGGINGFACE_API_KEY=hf_...
```

### 4. إعداد قاعدة البيانات

```bash
# إنشاء الجداول
npm run db:push

# إضافة البيانات الافتراضية
npm run db:seed
```

### 5. تشغيل النظام

#### الواجهة Web و API
```bash
npm run dev
```

#### البوت Telegram
```bash
cd telegram_bot
python main.py
```

## الاستخدام 📱

### إنشاء مهمة توجيه

1. افتح لوحة التحكم Web
2. اذهب إلى "المهام"
3. انقر "إنشاء مهمة جديدة"
4. حدد المصادر والأهداف
5. (اختياري) فعّل الذكاء الصناعي
6. (اختياري) فعّل معالجة الفيديو
7. احفظ المهمة

### إضافة قنوات/مجموعات

1. اذهب إلى "القنوات"
2. انقر "إضافة قناة"
3. أدخل معرف القناة أو رابطها
4. حدد نوع المصدر (قناة/مجموعة/موقع)
5. احفظ

### إضافة قواعد AI

1. افتح المهمة
2. اذهب إلى "قواعد الذكاء الصناعي"
3. انقر "إضافة قاعدة"
4. اختر نوع القاعدة (تلخيص/تحويل)
5. اكتب الـ prompt
6. احفظ

## أوامر البوت 🎯

### الأوامر الأساسية
- `/start` - بدء البوت
- `/help` - المساعدة
- `/stats` - الإحصائيات

### إدارة المهام
- `/list_tasks` - عرض المهام
- `/toggle_task [id]` - تفعيل/تعطيل مهمة
- `/task_stats [id]` - إحصائيات مهمة
- `/delete_task [id]` - حذف مهمة

### إدارة المشرفين
- `/add_admin [user_id]` - إضافة مشرف
- `/list_admins` - عرض المشرفين

## API Endpoints 🌐

### Authentication
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/register` - إنشاء حساب

### Tasks
- `GET /api/tasks` - جميع المهام
- `POST /api/tasks` - إنشاء مهمة
- `PATCH /api/tasks/:id` - تحديث مهمة
- `DELETE /api/tasks/:id` - حذف مهمة
- `POST /api/tasks/:id/toggle` - تفعيل/تعطيل
- `GET /api/tasks/:id/stats` - الإحصائيات
- `GET /api/tasks/:id/logs` - السجلات

### Channels
- `GET /api/channels` - جميع القنوات
- `POST /api/channels` - إضافة قناة
- `PATCH /api/channels/:id` - تحديث قناة
- `DELETE /api/channels/:id` - حذف قناة

### AI
- `GET /api/ai/providers` - مزودي AI
- `GET /api/ai/models` - موديلات AI
- `GET /api/tasks/:id/rules` - قواعد AI للمهمة
- `POST /api/tasks/:id/rules` - إضافة قاعدة
- `PATCH /api/rules/:id` - تحديث قاعدة
- `DELETE /api/rules/:id` - حذف قاعدة

## التحسينات والأداء ⚡

### نظام Workers
- معالجة متوازية بـ 10 workers
- نظام أولويات للمهام
- إعادة محاولة تلقائية عند الفشل

### التخزين المؤقت
- تخزين الرسائل المعالجة
- تجنب إعادة المعالجة
- تحديد حجم الذاكرة

### معالجة الأخطاء
- تسجيل شامل للأخطاء
- تتبع مفصل لكل وظيفة
- إشعارات تلقائية

## المساهمة 🤝

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء branch للميزة الجديدة
3. Commit التغييرات
4. Push إلى branch
5. فتح Pull Request

## الترخيص 📄

MIT License

## الدعم الفني 💬

للمساعدة والدعم، يرجى فتح Issue على GitHub.

---

صُنع بـ ❤️ للمجتمع العربي
